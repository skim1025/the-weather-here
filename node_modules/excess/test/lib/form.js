// Generated by CoffeeScript 1.7.1

/*
    xs_form.coffee

    Copyright (C) 2013, 2014, Connected Sets

    This program is free software: you can redistribute it and/or modify
    it under the terms of the GNU Affero General Public License as
    published by the Free Software Foundation, either version 3 of the
    License, or (at your option) any later version.

    This program is distributed in the hope that it will be useful,
    but WITHOUT ANY WARRANTY; without even the implied warranty of
    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
    GNU Affero General Public License for more details.

    You should have received a copy of the GNU Affero General Public License
    along with this program.  If not, see <http://www.gnu.org/licenses/>.
 */

(function() {
  var Browser, browser, check, expect, utils;

  Browser = require('zombie');

  utils = require('./tests_utils.js');

  browser = new Browser({
    silent: true
  });

  expect = this.expect || utils.expect;

  check = this.check || utils.check;

  describe('Form() Test Suite', function() {
    before(function(done) {
      return browser.visit('http://localhost:8080/test/xs_form.html', done);
    });
    it('body element should have a "form" element', function() {
      return expect(browser.queryAll('form').length).to.be(1);
    });
    it('form should have a hidden field with name="flow" and value="user_profile"', function() {
      var field;
      field = browser.field('input[name="flow"]');
      expect(field).to.not.be(void 0);
      expect(field.type).to.be('hidden');
      expect(field.name).to.be('flow');
      return expect(field.value).to.be('user_profile');
    });
    it('form should have a hidden field with name="id"', function() {
      var field;
      field = browser.field('input[name="id"]');
      expect(field).to.not.be(void 0);
      expect(field.type).to.be('hidden');
      return expect(field.name).to.be('id');
    });
    it('form should have a field with name="name", type="text"', function() {
      var field;
      field = browser.field('input[name="name"]');
      expect(field).to.not.be(void 0);
      expect(field.type).to.be('text');
      return expect(field.name).to.be('name');
    });
    it('form should have a textarea with name="address", cols="30" and rows="5"', function() {
      var field;
      field = browser.field('textarea');
      expect(field).to.not.be(void 0);
      expect(field.name).to.be('address');
      expect(field.cols).to.be(30);
      return expect(field.rows).to.be(5);
    });
    return it('form should have a disabled button with name="form_submit" and value="OK"', function() {
      var field;
      field = browser.button('input[type="submit"]');
      expect(field).to.not.be(void 0);
      expect(field.disabled).to.be(true);
      expect(field.name).to.be('form_submit');
      return expect(field.value).to.be('OK');
    });
  });

}).call(this);

//# sourceMappingURL=form.map
